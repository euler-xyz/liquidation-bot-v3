use alloy::primitives::{Address, U256};
use std::{collections::HashMap, sync::Arc};

mod accounts;
mod subgraph;
mod types;

// type Oracles = HashMap<OracleIdentifier, Vec<Account>>;

#[tokio::main]
async fn main() {}
